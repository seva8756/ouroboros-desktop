openai>=1.66.0
requests
dulwich
starlette
uvicorn[standard]
websockets
huggingface_hub
# optional: browser automation tools (for browse_page/browser_action)
playwright
playwright-stealth
# optional: local model server (CPU-only by default; for GPU add
# CMAKE_ARGS="-DGGML_METAL=on" before pip install on macOS)
llama-cpp-python[server]
